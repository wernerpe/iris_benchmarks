from setuptools import setup, find_packages

setup(
    name='iris_environments',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # list your dependencies here
    ],
)